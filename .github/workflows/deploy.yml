name: Deploy API-jarvis

on:
  push:
    branches:
      - main

jobs:
  deploy:
    runs-on: self-hosted

    steps:
      - name: Atualizar código e reiniciar API do jarvis
        run: |
          cd /home/gabz-admin/apis/jarvis/jarvis-home-server
          git fetch origin
          git reset --hard origin/main